using Photon.Pun;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FalconTrade : MonoBehaviour
{
    private bool isCalled;
    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void useFalcon()
    {
        isCalled = true;
        //if (PhotonNetwork.LocalPlayer.GetHero().data.falcon == 1 && !PhotonNetwork.LocalPlayer.GetHero().data.usedFalcon ) // user has falcon and it's not veing used
        //{

        //}
    }
}
